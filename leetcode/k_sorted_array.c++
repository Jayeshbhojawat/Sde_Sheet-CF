/* array is sorted in the k interval 
// so we qill maintain an min heap of 
size =k ; so that when size become equal to heap size than 
we can pop top element which is the min 
and than again we push from the array to the min heap 
and again check for heap size condn
continue this untill we reach the last l=element of heap 
haha
completed 
//fuck u bitch
*/
